# SAP Cloud SDK - includes agent_memory module (E8)
sap-cloud-sdk[langgraph]~=0.25

# LiteLLM + LangChain + LangGraph
litellm==1.89.3
langchain==1.3.10
langchain-core==1.4.8
langchain-litellm==0.7.0
langgraph==1.2.6

# A2A protocol
a2a-sdk[http-server]==0.3.22

# HTTP + async runtime
uvicorn==0.40.0
httpx==0.28.1

# Claude fallback path (Hyperspace proxy)
anthropic

# Output rendering
python-docx

# .env.local loader
python-dotenv

# v3.0 additions (E4 - Prompt Registry)
jinja2>=3.1.0
PyYAML>=6.0